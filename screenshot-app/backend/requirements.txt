fastapi==0.120.4
uvicorn==0.38.0
playwright==1.55.0
playwright-stealth==1.0.6
python-docx==1.2.0
pillow==12.0.0
websockets==15.0.1
pydantic==2.12.3
pydantic-settings==2.1.0
imagehash==4.3.1
cachetools==5.3.2  # ✅ TTL cache for memory leak prevention

# ✅ 2025 STEALTH ENHANCEMENTS (Priority Order)

# 1. Patchright - BEST (patches CDP leaks at source level)
# Bypasses Runtime.enable, Console.enable, and all CDP detection
patchright>=1.56.0

# 2. Rebrowser - GOOD (CDP patches, fallback if Patchright fails)
rebrowser-playwright>=1.55.0

# 3. Camoufox - OPTIONAL (Firefox-based maximum stealth)
# Custom Firefox build with TLS fingerprint patches (90-95% success on protected sites)
# [geoip] extra provides IP-based geolocation for locale/timezone spoofing
camoufox[geoip]>=0.4.0

